def  pow(base, exp):
    """This function count the exp pow of base"""
    if base > 0 and exp > 0:
        return base**exp
    elif base > 0 and exp < 0
          return 1/base**(-exp)
    elif base > 0 and exp==0
        return 1
  print pow(3, -3)
