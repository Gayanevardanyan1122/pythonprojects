def contain(data, value):
    """this function reurns true when the data list contains value and returns false when it doesn't contain"""
    i=0
    while i < len(data):
         if data[i] == value:
             return true
         i += 1
     reurn false 
 print  contain(data, 2)
