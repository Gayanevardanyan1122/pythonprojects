data = [1, 22, 56, 4]
def remove_all(data, value):
    """This function removes  members that equal to value."""
    index = 0
    while  index < len(data):
         if data[index] = value
             data.remove(index)
          index += 1
     return  data
